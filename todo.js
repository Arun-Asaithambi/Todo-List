let todoList = document.getElementById('todolist')   
let input = document.getElementsById("input")
let add = document.getElementById('add')

let todos = [];

butoon.addEventListner('click', () => {
    todos.push(input)
    addtodo(input.value)
})

function addtodo(todo){

}